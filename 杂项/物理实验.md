```cpp
    // const double r2 = 8.0 / 1000 * 8 / 1000;
    // const double l = 14.0 / 100;
    // const double s = 46.04 * 1e-6;
    // double a, b;cin >> a >> b;
    // const double tt = 0.05 * 10*1e-3 / 860 / s;
    // cout << scientific<<tt * a / b << endl;
    // double x;cin >> x;x /= 100;
    // cout <<scientific<< 4 * 3.14159*1e-7 * 2470*0.4 / 2 / (l*2) * 
    //     ((l - x) / sqrtl(pow(l - x, 2) + r2) + (l + x) / sqrtl(pow(l + x, 2) + r2))<<endl;
    double t[] = { 2.5,3.42,3.93,4.17,4.31,4.48,4.55,4.65,4.65 };
    double a[] = { 2.21,3.39,3.94,4.17,4.27,4.38,4.42,4.42,4.42 };
    for (int i = 0;i < 9;i++) {
        cout << fabs(t[i] - a[i])/a[i] * 100 << endl;
    }
```